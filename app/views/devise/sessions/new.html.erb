<div class="login-page">
  <div class="sign-in-box">
    <h2 class="header">Sign In</h2>
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "email", :class => "input" %>
      </div>

      <div>
        <%= f.password_field :password, autocomplete: "current-password", placeholder: "password", :class => "input" %>
      </div>

      <%= link_to "forgot your password?", new_password_path(resource_name), :class => "forgot-pwd" %>

      <div class="remember-me">
        <%= f.check_box :remember_me%>
        <label>remember me</label>
      </div>

      <div>
        <%= f.submit "sign in", :class => "sign-in-button account-button"%>
      </div>

      <div class="sign-up account-link">
        <span>don’t have an account?</span>
        <%= link_to "sign up", new_registration_path(resource_name) %>
      </div>
    <% end %>
  </div>
</div>
