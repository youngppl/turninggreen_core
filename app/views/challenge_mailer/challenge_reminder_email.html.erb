<head>

  <link href="https://fonts.googleapis.com/css2?family=Raleway:wght@100;200;300;400;500;600;700;800;900&display=swap"
    rel="stylesheet">
</head>

<body>
  <%= image_tag 'rootup-logo.png' %>

  <h1 class="name">Hey <%= @user.name %>!</h1>

  <h2 class="title">
    We’re back, delivering your email dose of
  </h2>
  <h2 class="title bold uppercase">
    “IF YA DON'T KNOW, NOW YA KNOW”:
  </h2>
  <h2 class="subtitle"><%= @fact %></h2>
  
  <% if @updates %>
    <hr>
    <h2 class="title bold uppercase">Here's the rootup scoop:</h2>
    <% @updates.each do |update| %>
      <h2 class="title"><%= update.html_safe %> </h2>
    <% end %>
  <% end %>

  <hr>

  <h2 class="title bold uppercase">Keep kicking it with us by:</h2>
  <ul>
    <li>Learning + doing something <a target="_blank" href=" <%= themes_url %> ">right now</a> <span>(it
        only takes a minute!)</span></li>
    <li><a target="_blank" href=" <%= dashboard_url %> ">Show off</a> your impact <span>(cause your
        actions are worth bragging about!)</span></li>
    <li><a target="_blank" href=" <%= challenges_reflections_url %> ">Share</a> your experiences <span>(cause we want to know!)</span></li>
  </ul>

  <hr>

  <h2 class="title bold uppercase">Never forget that every action matters(!)</h2>
  <h2 class="title">Keep challenging yourself to do and do more:</h2>

  <ol>
    <% @user_completed_challenges.each do |challenge| %>
    <li> <%= challenge.challenge_name %> (complete)</li>
    <% end %>
    <% @user_incomplete_challenges.each do |challenge| %>
    <li> <%= challenge.challenge_name %> (ends in <%= challenge.get_time_left %>)</li>
    <% end %>
  </ol>

  <hr>

  <h2 class="title bold uppercase" style="max-width: 530px;margin: auto;">We want to serve you and the environment better.</h2>
  <h2 class="title"> Hit reply if you’ve got any feedback to share. We accept all
    positives and deltas (room for improvement) :)</h2>

  <h2 class="title closing">
    Rootin’ for ya!<br>
    the Rootup team
  </h2>

  <p>P.S. <a target="_blank" href="<%= edit_user_registration_url %> ">Update</a> email reminder settings or <a
      target="_blank" href="<%= unsubscribe_url(email: @user.email) %>">unsubscribe</a></p>
</body>
<style>
  body {
    background-color: #f7f7f2;
    max-width: 880px;
    margin: auto;
    font-family: Raleway, sans-serif;
    font-size: 25px;
    font-weight: 500;
    text-align: center;
    color: #474747;
    padding: 30px;
  }

  .name {
    font-size: 45px;
    font-weight: bold;
    letter-spacing: -0.69px;
    margin: 50px 0;
  }

  .title {
    font-size: 30px;
    font-weight: 500;
    margin: 0;
  }

  .subtitle {
    font-size: 27px;
    font-weight: 500;
  }

  .small {
    font-size: 11px;
  }

  .bold {
    font-weight: bold !important;
  }

  .uppercase {
    text-transform: uppercase;
  }

  .closing {
    margin: 30px 0;
  }

  hr {
    width: 300px;
    margin-top: 50px;
    margin-bottom: 50px;
  }

  a {
    text-decoration: underline;
  }

  ul, ol {
    text-align: left;
  }

  ol {
    width: fit-content;
    margin: 30px auto;
  }

  p {
    font-size: 20px;
  }
</style>